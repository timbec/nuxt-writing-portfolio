{
  "name": "nuxt-writing-portfolio",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "node-sass": "^7.0.1"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "watch": "node-sass assets/sass/ -o ./ --source-map true --output-style expanded --indent-type tab --indent-width 1 -w"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7"
  }
}